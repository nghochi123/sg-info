import React from 'react';

import Layout from '../../components/Layout/Layout';

import './Home.css'

const Home = (props) => (
    <div>
        <Layout>
            <p className="HomeText">Random app created with React to retrieve data from Singapore's data APIs</p>
        </Layout>
    </div>
    
    
);

export default Home;